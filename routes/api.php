<?php

use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| Here is where you can register API routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| is assigned the "api" middleware group. Enjoy building your API!
|
*/

    // Route::middleware('auth:sanctum')->get('/user', function (Request $request) {
    //     return $request->user();
    // });
    // Route::middleware('cors')->group(function () {

    //     Route::post('/login', [\App\Http\Controllers\AuthController::class, 'login']);
    //     Route::get('/services', [\App\Http\Controllers\ServiceController::class, 'index']);
    // });
    // Route::middleware(['cors', 'auth:sanctum'])->group(function () {
    //     Route::put('/change-password', [\App\Http\Controllers\UserController::class, 'ChangePassword'])->middleware('admin');
    //     Route::put('/change-email', [\App\Http\Controllers\UserController::class, 'ChangeEmail'])->middleware('admin');
    //     Route::post('/logout', [\App\Http\Controllers\AuthController::class, 'logout']);
    //     Route::post('/services/store', [\App\Http\Controllers\ServiceController::class, 'store']);
    // });
    // Route::middleware(['cors', 'auth:sanctum'])->group(function () {
    //     Route::put('/service/update/{id}', [\App\Http\Controllers\ServiceController::class, 'edit'])->name('service.update')->middleware('admin');
    //     Route::delete('/service/delete/{id}', [\App\Http\Controllers\ServiceController::class, 'destroy'])->name('service.delete')->middleware('admin');
    // });
